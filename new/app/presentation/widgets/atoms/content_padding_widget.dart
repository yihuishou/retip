import 'package:flutter/material.dart';

class ContentPaddingWidget extends StatelessWidget {
  const ContentPaddingWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.square(
      dimension: 8,
    );
  }
}
